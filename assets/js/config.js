// Site settings — the only file you may need to edit by hand.
export const SITE = {
  version: '1.0.1',
  // The GitHub repository that hosts this site and the encrypted library.
  owner: 'willyros01',
  repo: 'Cuberoot',
  // Contact address shown in the footer and on the sign-in page.
  // Leave empty ('') to hide the Contact links.
  contactEmail: '',
  // Library locks itself after this many minutes without use.
  lockMinutes: 15,
};
